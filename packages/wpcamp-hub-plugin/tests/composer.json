{
	"name": "wpcamp-hub/test-dependencies",
	"description": "Test-only dependencies for wpcamp-hub. Add premium or private plugins here.",
	"require": {
		"composer/installers": "^2.0"
	},
	"extra": {
		"installer-paths": {
			"setup/plugins/{$name}/": [
				"type:wordpress-plugin"
			]
		}
	},
	"config": {
		"allow-plugins": {
			"composer/installers": true
		}
	}
}